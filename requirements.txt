# Fleet Monitor — runtime dependencies for the pipeline (GHA + local).
# Pinned to known-good majors.

# Core pipeline
httpx>=0.27,<1.0
tenacity>=8.2,<10.0
pyyaml>=6.0,<7.0
python-dotenv>=1.0,<2.0

# Analytics lake (parquet + DuckDB rollups in analytics_pull.py)
pyarrow>=15.0,<22.0
duckdb>=1.0,<2.0

# Google Analytics 4 + Search Console pulls (skills/analytics/*_pull.py)
google-analytics-data>=0.18,<1.0
google-api-python-client>=2.100,<3.0
google-auth-oauthlib>=1.0,<2.0
google-auth-httplib2>=0.2,<1.0

# R2 state sync (r2_state.py)
boto3>=1.34,<2.0
