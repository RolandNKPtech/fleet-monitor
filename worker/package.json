{
  "name": "fleet-monitor-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker — serves fleet monitor HTML from R2 + dispatches GHA pipeline runs",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4",
    "typescript": "^5",
    "wrangler": "^3"
  }
}
